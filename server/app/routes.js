// create a new express router
const express       = require('express'),
    router          = express.Router();

// export the router
module.exports = router;

// define the routes
// users route
router.get('/users');